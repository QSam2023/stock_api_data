# Test & Fix Bugs TodoList

## P0（立即处理）

- [ ] 修复 `analyze.py` 资金流近 5 日取值方向错误
  - 问题：当前使用 `head(5)`，实际应取最新 5 日（`tail(5)`）。
  - 影响：输出“近 5 日主力净流入”与真实最新区间不一致。
  - 验收：`TC-ANL-004` 通过。

- [ ] 修复 `masters_indicators.py` 首次拉取 CSV 返回路径错误
  - 问题：`find_or_fetch_csv()` 返回 `fetch_kline.py` 的整段 stdout，而不是文件路径。
  - 影响：首次无本地缓存时可能 `pd.read_csv()` 失败。
  - 验收：删除目标 `data/*.csv` 后执行 `TC-MST-001` 通过。

- [ ] 增加“网页对比基线快照”记录
  - 内容：每次测试记录网页来源 URL、抓取时间、关键字段。
  - 影响：避免因网页刷新造成回归不可复现。
  - 验收：每个 P0 用例都有快照证据。

## P1（本周完成）

- [ ] 为 `fetch_kline.py` 增加日期参数格式校验
  - 验收：非法日期（如 `20261301`）返回非 0 且提示明确。

- [ ] 为 `analyze.py` 增加资金流列名白名单
  - 内容：优先匹配 `主力净流入-净额`，避免误命中“净占比”。
  - 验收：`TC-ANL-003/004` 在列顺序变化时仍稳定。

- [ ] 为 `masters_indicators.py` 添加单元测试
  - 范围：Step1/Step2/Step3 的边界场景（数据不足、极值、无成交量异常）。
  - 验收：新增测试全部通过。

- [ ] 增加网络异常重试与超时策略
  - 范围：AKShare 拉取失败时有限重试、错误分类提示。
  - 验收：瞬时网络抖动不再直接失败。

## P2（迭代优化）

- [ ] 引入统一测试入口（如 `scripts/run_regression.sh`）
  - 包含：P0/P1 用例一键执行、汇总报告生成。

- [ ] 增加跨数据源差异解释模板
  - 场景：复权差异、成交量口径、停牌日处理。

- [ ] 将测试结果汇总写入 `output/test_reports/` 并按日期归档
  - 产物：`summary.md` + 原始日志。

## 每轮执行顺序

1. 执行 `TC-FETCH-001/002`（先确认基础行情链路可用）。
2. 执行 `TC-ANL-001~004`（定位资金流和指标口径问题）。
3. 执行 `TC-MST-001~003`（确认策略流程可跑且结论稳定）。
4. 执行 `TC-PLOT-001/002`（确认产物和参数校验）。
5. 更新 README 版本历史与测试报告索引。

